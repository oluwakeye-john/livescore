import 'package:flutter/material.dart';

class MatchDetailsLineup extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return (Center(
      child: Image.asset('assets/lineup.png'),
    ));
  }
}
